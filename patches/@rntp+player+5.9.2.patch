diff --git a/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerCastMediaItemConverter.kt b/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerCastMediaItemConverter.kt
index 2518f2c..02a86ad 100644
--- a/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerCastMediaItemConverter.kt
+++ b/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerCastMediaItemConverter.kt
@@ -64,6 +64,9 @@ internal class TrackPlayerCastMediaItemConverter : MediaItemConverter {
         if (extras.containsKey(KEY_IS_LIVE)) {
           payload.put(KEY_IS_LIVE, extras.getBoolean(KEY_IS_LIVE))
         }
+        if (extras.containsKey(KEY_END_POSITION)) {
+          payload.put(KEY_END_POSITION, extras.getDouble(KEY_END_POSITION))
+        }
         extras.getBundle(MediaItemExtras.METADATA_KEY)?.let { appExtras ->
           payload.put(KEY_EXTRAS, bundleToJson(appExtras))
         }
@@ -98,6 +101,9 @@ internal class TrackPlayerCastMediaItemConverter : MediaItemConverter {
     if (rntp.has(KEY_IS_LIVE) && !metadataExtras.containsKey(KEY_IS_LIVE)) {
       metadataExtras.putBoolean(KEY_IS_LIVE, rntp.getBoolean(KEY_IS_LIVE))
     }
+    if (rntp.has(KEY_END_POSITION) && !metadataExtras.containsKey(KEY_END_POSITION)) {
+      metadataExtras.putDouble(KEY_END_POSITION, rntp.getDouble(KEY_END_POSITION))
+    }
     if (rntp.has(KEY_EXTRAS) && !metadataExtras.containsKey(MediaItemExtras.METADATA_KEY)) {
       MediaItemExtras.attachToMetadataExtras(
         metadataExtras,
@@ -106,7 +112,15 @@ internal class TrackPlayerCastMediaItemConverter : MediaItemConverter {
     }
 
     metaBuilder.setExtras(metadataExtras)
-    return item.buildUpon().setMediaMetadata(metaBuilder.build()).build()
+    val itemBuilder = item.buildUpon().setMediaMetadata(metaBuilder.build())
+    if (rntp.has(KEY_END_POSITION)) {
+      itemBuilder.setClippingConfiguration(
+        MediaItem.ClippingConfiguration.Builder()
+          .setEndPositionMs((rntp.getDouble(KEY_END_POSITION) * 1000).toLong())
+          .build()
+      )
+    }
+    return itemBuilder.build()
   }
 
   private fun copyCustomData(customData: JSONObject?): JSONObject =
@@ -119,6 +133,7 @@ internal class TrackPlayerCastMediaItemConverter : MediaItemConverter {
     const val KEY_ARTWORK_URL = "artworkUrl"
     const val KEY_DURATION = "duration"
     const val KEY_IS_LIVE = "isLive"
+    const val KEY_END_POSITION = "endPosition"
     const val KEY_EXTRAS = "extras"
 
     fun bundleToJson(bundle: Bundle): JSONObject {
diff --git a/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerMediaItem.kt b/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerMediaItem.kt
index 43b2934..6ed670f 100644
--- a/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerMediaItem.kt
+++ b/node_modules/@rntp/player/android/src/main/java/com/doublesymmetry/trackplayer/models/TrackPlayerMediaItem.kt
@@ -24,6 +24,7 @@ data class TrackPlayerMediaItem(
   val albumTitle: String? = null,
   val artworkUrl: String? = null,
   val duration: Double? = null,
+  val endPosition: Double? = null,
   val isLive: Boolean? = null,
   val mimeType: String? = null,
   val headers: Map<String, String>? = null,
@@ -46,6 +47,7 @@ data class TrackPlayerMediaItem(
 
     val metadataExtras = Bundle()
     duration?.let { metadataExtras.putDouble("duration", it) }
+    endPosition?.let { metadataExtras.putDouble("endPosition", it) }
     isLive?.let { metadataExtras.putBoolean("isLive", it) }
     extras?.let { MediaItemExtras.attachToMetadataExtras(metadataExtras, it) }
 
@@ -85,6 +87,15 @@ data class TrackPlayerMediaItem(
         MediaItem.LiveConfiguration.Builder().build()
       )
     }
+    endPosition
+      ?.takeIf { it.isFinite() && it > 0 }
+      ?.let {
+        builder.setClippingConfiguration(
+          MediaItem.ClippingConfiguration.Builder()
+            .setEndPositionMs((it * 1000).toLong())
+            .build()
+        )
+      }
 
     val mediaItem = builder.build()
     headers?.let { MediaHeaders.set(resolvedUrl, it) }
@@ -100,6 +111,7 @@ data class TrackPlayerMediaItem(
     albumTitle?.let { map.putString("albumTitle", it) }
     artworkUrl?.let { map.putString("artworkUrl", it) }
     duration?.let { map.putDouble("duration", it) }
+    endPosition?.let { map.putDouble("endPosition", it) }
     isLive?.let { map.putBoolean("isLive", it) }
     extras?.let { map.putMap("extras", Arguments.fromBundle(it)) }
     return map
@@ -173,6 +185,7 @@ data class TrackPlayerMediaItem(
         albumTitle = if (map.hasKey("albumTitle")) map.getString("albumTitle") else null,
         artworkUrl = if (map.hasKey("artworkUrl")) map.getString("artworkUrl") else null,
         duration = if (map.hasKey("duration")) map.getDouble("duration") else null,
+        endPosition = if (map.hasKey("endPosition")) map.getDouble("endPosition") else null,
         isLive = if (map.hasKey("isLive")) map.getBoolean("isLive") else null,
         mimeType = if (map.hasKey("mimeType")) map.getString("mimeType") else null,
         headers = headers,
@@ -191,6 +204,9 @@ data class TrackPlayerMediaItem(
         albumTitle = metadata.albumTitle?.toString(),
         artworkUrl = metadata.artworkUri?.toString(),
         duration = metadataExtras?.getDouble("duration"),
+        endPosition = metadataExtras
+          ?.takeIf { it.containsKey("endPosition") }
+          ?.getDouble("endPosition"),
         isLive = if (metadataExtras?.containsKey("isLive") == true) metadataExtras.getBoolean("isLive") else null,
         extras = metadataExtras?.getBundle(MediaItemExtras.METADATA_KEY),
       )
diff --git a/node_modules/@rntp/player/ios/models/MediaItem.swift b/node_modules/@rntp/player/ios/models/MediaItem.swift
index c38942a..b928f46 100644
--- a/node_modules/@rntp/player/ios/models/MediaItem.swift
+++ b/node_modules/@rntp/player/ios/models/MediaItem.swift
@@ -15,10 +15,11 @@ struct MediaItem {
   let albumTitle: String?
   let artworkUrl: MediaURL?
   let duration: Double?
+  let endPosition: Double?
   let isLiveStream: Bool?
   let extras: [String: Any]?
 
-  init(mediaId: String, url: MediaURL, title: String?, artist: String?, albumTitle: String?, artworkUrl: MediaURL?, duration: Double? = nil, isLiveStream: Bool? = nil, extras: [String: Any]? = nil) {
+  init(mediaId: String, url: MediaURL, title: String?, artist: String?, albumTitle: String?, artworkUrl: MediaURL?, duration: Double? = nil, endPosition: Double? = nil, isLiveStream: Bool? = nil, extras: [String: Any]? = nil) {
     self.mediaId = mediaId
     self.url = url
     self.title = title
@@ -26,6 +27,7 @@ struct MediaItem {
     self.albumTitle = albumTitle
     self.artworkUrl = artworkUrl
     self.duration = duration
+    self.endPosition = endPosition
     self.isLiveStream = isLiveStream
     self.extras = extras
   }
@@ -41,10 +43,11 @@ struct MediaItem {
     let albumTitle = data["albumTitle"] as? String
     let artworkUrl = MediaURL(object: data["artworkUrl"])
     let duration = data["duration"] as? Double
+    let endPosition = data["endPosition"] as? Double
     let isLiveStream = data["isLive"] as? Bool
     let extras = data["extras"] as? [String: Any]
 
-    self.init(mediaId: mediaId, url: url, title: title, artist: artist, albumTitle: albumTitle, artworkUrl: artworkUrl, duration: duration, isLiveStream: isLiveStream, extras: extras)
+    self.init(mediaId: mediaId, url: url, title: title, artist: artist, albumTitle: albumTitle, artworkUrl: artworkUrl, duration: duration, endPosition: endPosition, isLiveStream: isLiveStream, extras: extras)
   }
 }
 
@@ -63,6 +66,7 @@ extension MediaItem {
       albumTitle: albumTitle ?? self.albumTitle,
       artworkUrl: artworkUrl ?? self.artworkUrl,
       duration: self.duration,
+      endPosition: self.endPosition,
       isLiveStream: self.isLiveStream,
       extras: self.extras
     )
@@ -80,6 +84,7 @@ extension MediaItem {
       dict["artworkUrl"] = artworkUrl.isLocal ? artworkUrl.value.path : artworkUrl.value.absoluteString
     }
     if let duration = duration { dict["duration"] = duration }
+    if let endPosition = endPosition { dict["endPosition"] = endPosition }
     if let isLiveStream = isLiveStream { dict["isLive"] = isLiveStream }
     if let extras = extras { dict["extras"] = extras }
     return dict
diff --git a/node_modules/@rntp/player/ios/player/AVPlayerEngine.swift b/node_modules/@rntp/player/ios/player/AVPlayerEngine.swift
index edc06f0..5b1ba66 100644
--- a/node_modules/@rntp/player/ios/player/AVPlayerEngine.swift
+++ b/node_modules/@rntp/player/ios/player/AVPlayerEngine.swift
@@ -72,6 +72,7 @@ class AVPlayerEngine: NSObject, PlayerEngine, AVPlayerItemMetadataOutputPushDele
   private var activeSource: SourceContext?
   private var liveRefresh: LiveRefresh?
   private var metadataOutputGeneration: Int?
+  private var activeEndPosition: Double?
 
   // Internal deterministic seam used by PlayerCoreTests. Production instances
   // leave both closures nil and exercise AVFoundation directly.
@@ -127,17 +128,17 @@ class AVPlayerEngine: NSObject, PlayerEngine, AVPlayerItemMetadataOutputPushDele
 
   var duration: Double {
     if let seconds = avPlayer.currentItem?.duration.seconds, !seconds.isNaN {
-      return seconds
+      return clippedPosition(seconds)
     }
     if let range = avPlayer.currentItem?.seekableTimeRanges.last?.timeRangeValue,
        !range.duration.seconds.isNaN {
-      return range.duration.seconds
+      return clippedPosition(range.duration.seconds)
     }
-    return 0
+    return activeEndPosition ?? 0
   }
 
   var bufferedPosition: Double {
-    avPlayer.currentItem?.loadedTimeRanges.last?.timeRangeValue.end.seconds ?? 0
+    clippedPosition(avPlayer.currentItem?.loadedTimeRanges.last?.timeRangeValue.end.seconds ?? 0)
   }
 
   var cachedPosition: Double {
@@ -256,8 +257,11 @@ class AVPlayerEngine: NSObject, PlayerEngine, AVPlayerItemMetadataOutputPushDele
     avPlayer.pause()
   }
 
-  func load(url: URL, headers: [String: String]? = nil, isLive: Bool = false) {
+  func load(url: URL, headers: [String: String]? = nil, isLive: Bool = false, endPosition: Double? = nil) {
     cancelLiveRefresh()
+    activeEndPosition = endPosition.flatMap {
+      $0.isFinite && $0 > 0 ? $0 : nil
+    }
     activeSource = SourceContext(url: url, headers: headers, isLive: isLive)
     loadSource(url: url, headers: headers, isLive: isLive)
   }
@@ -318,6 +322,9 @@ class AVPlayerEngine: NSObject, PlayerEngine, AVPlayerItemMetadataOutputPushDele
   private func installItem(_ item: AVPlayerItem) {
     loadGeneration &+= 1
     removeItemObservers()
+    if let endPosition = activeEndPosition {
+      item.forwardPlaybackEndTime = CMTime(seconds: endPosition, preferredTimescale: 1000)
+    }
     avPlayer.replaceCurrentItem(with: item)
     observeItem(item, generation: loadGeneration)
   }
@@ -366,7 +373,8 @@ class AVPlayerEngine: NSObject, PlayerEngine, AVPlayerItemMetadataOutputPushDele
   }
 
   func seek(to seconds: TimeInterval, completion: @escaping (Bool) -> Void) {
-    let time = CMTimeMakeWithSeconds(seconds, preferredTimescale: 1000)
+    let position = activeEndPosition.map { min(seconds, $0) } ?? seconds
+    let time = CMTimeMakeWithSeconds(position, preferredTimescale: 1000)
     avPlayer.seek(to: time, toleranceBefore: .zero, toleranceAfter: .zero) { finished in
       DispatchQueue.main.async { completion(finished) }
     }
@@ -410,6 +418,7 @@ class AVPlayerEngine: NSObject, PlayerEngine, AVPlayerItemMetadataOutputPushDele
 
   func reset() {
     activeSource = nil
+    activeEndPosition = nil
     cancelLiveRefresh()
     currentCacheKey = nil
     pendingDirectFallback = nil
@@ -420,6 +429,11 @@ class AVPlayerEngine: NSObject, PlayerEngine, AVPlayerItemMetadataOutputPushDele
     avPlayer.replaceCurrentItem(with: nil)
   }
 
+  private func clippedPosition(_ position: Double) -> Double {
+    guard position.isFinite, position >= 0 else { return 0 }
+    return activeEndPosition.map { min(position, $0) } ?? position
+  }
+
   // MARK: - Item Failure Handling
 
   /// Single choke point for item-failure surfacing. If a proxied load is still
diff --git a/node_modules/@rntp/player/ios/player/AudioItem.swift b/node_modules/@rntp/player/ios/player/AudioItem.swift
index c95d040..e2fb32e 100644
--- a/node_modules/@rntp/player/ios/player/AudioItem.swift
+++ b/node_modules/@rntp/player/ios/player/AudioItem.swift
@@ -22,6 +22,8 @@ protocol AudioItem {
   var albumTitle: String? { get }
   /// App-provided duration in seconds, if known ahead of playback.
   var duration: Double? { get }
+  /// Native playback boundary in seconds. Playback ends normally at this time.
+  var endPosition: Double? { get }
   #if canImport(UIKit)
   func getArtwork(_ handler: @escaping (UIImage?) -> Void)
   #endif
@@ -29,4 +31,5 @@ protocol AudioItem {
 
 extension AudioItem {
   var duration: Double? { nil }
+  var endPosition: Double? { nil }
 }
diff --git a/node_modules/@rntp/player/ios/player/AudioPlayer.swift b/node_modules/@rntp/player/ios/player/AudioPlayer.swift
index 30e5ef9..6045e6a 100644
--- a/node_modules/@rntp/player/ios/player/AudioPlayer.swift
+++ b/node_modules/@rntp/player/ios/player/AudioPlayer.swift
@@ -216,7 +216,12 @@ class AudioPlayer {
 
     guard let itemUrl = url else { return }
 
-    engine.load(url: itemUrl, headers: item.headers, isLive: item.isLive)
+    engine.load(
+      url: itemUrl,
+      headers: item.headers,
+      isLive: item.isLive,
+      endPosition: item.endPosition
+    )
     state = .loading
   }
 
@@ -456,7 +461,12 @@ class AudioPlayer {
       artworkUri: nil,
       genre: nil
     )
-    engine.load(url: itemUrl, headers: item.headers, isLive: item.isLive)
+    engine.load(
+      url: itemUrl,
+      headers: item.headers,
+      isLive: item.isLive,
+      endPosition: item.endPosition
+    )
     state = .loading
     resetNowPlaying(for: item)
     onCurrentItemChanged?(item, queue.currentIndex)
diff --git a/node_modules/@rntp/player/ios/player/PlayerEngine.swift b/node_modules/@rntp/player/ios/player/PlayerEngine.swift
index 3c9d172..698a187 100644
--- a/node_modules/@rntp/player/ios/player/PlayerEngine.swift
+++ b/node_modules/@rntp/player/ios/player/PlayerEngine.swift
@@ -52,7 +52,7 @@ protocol PlayerEngine: AnyObject {
 
   func play()
   func pause()
-  func load(url: URL, headers: [String: String]?, isLive: Bool)
+  func load(url: URL, headers: [String: String]?, isLive: Bool, endPosition: Double?)
   func seek(to seconds: TimeInterval, completion: @escaping (Bool) -> Void)
   func seekToLiveEdge(completion: @escaping (Bool) -> Void)
   func reset()
diff --git a/node_modules/@rntp/player/lib/typescript/src/interfaces/MediaItem.d.ts b/node_modules/@rntp/player/lib/typescript/src/interfaces/MediaItem.d.ts
index 3689842..ee4f64d 100644
--- a/node_modules/@rntp/player/lib/typescript/src/interfaces/MediaItem.d.ts
+++ b/node_modules/@rntp/player/lib/typescript/src/interfaces/MediaItem.d.ts
@@ -45,6 +45,11 @@ export interface MediaItem {
      * Duration in seconds (optional hint for UI).
      */
     duration?: number;
+    /**
+     * Stop playback at this position in seconds. Unlike `duration`, this is a
+     * native clipping boundary and emits the normal end-of-item event.
+     */
+    endPosition?: number;
     /**
      * Whether this is a live stream.
      *
diff --git a/node_modules/@rntp/player/src/interfaces/MediaItem.ts b/node_modules/@rntp/player/src/interfaces/MediaItem.ts
index 8d912ae..e13e409 100644
--- a/node_modules/@rntp/player/src/interfaces/MediaItem.ts
+++ b/node_modules/@rntp/player/src/interfaces/MediaItem.ts
@@ -50,6 +50,11 @@ export interface MediaItem {
    * Duration in seconds (optional hint for UI).
    */
   duration?: number;
+  /**
+   * Stop playback at this position in seconds. Unlike `duration`, this is a
+   * native clipping boundary and emits the normal end-of-item event.
+   */
+  endPosition?: number;
   /**
    * Whether this is a live stream.
    *
