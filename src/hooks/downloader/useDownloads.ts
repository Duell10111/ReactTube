import {useState} from "react";

import {ElementData} from "@/extraction/Types";

export default function useDownloads() {
  const [data, setData] = useState<ElementData>();

  // TODO: Add Downloads switch labels between playlists/album/video downloads
}
